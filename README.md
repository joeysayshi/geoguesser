# geoguesser-next (Next.js 14 scaffold)

This is a manual Next.js 14 TypeScript scaffold because Node/npm are not available in this environment.

## When Node is installed

1. `cd geoguesser-next`
2. `npm install`
3. `npm run dev`

Routes:
- `/login`
- `/home`
- `/game`
